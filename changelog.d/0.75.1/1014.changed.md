- **The v0.72.2 8B NF4 throughput protocol is now re-runnable on repaired code, but the post-repair number is still missing (#361 by @umran666 in #1014).**
  No post-repair tok/s / peak VRAM / util / SM clock exists yet: the RTX 3050
  run has not happened, and nothing here invents one. `benchmarks/harness/
  issue361_nf4_throughput.py` repeats the step-6 protocol unchanged (batch 1,
  S=512, 50 steps after 10 warm-up, `PagedAdamW8bit`, same-session GEMM
  ceiling) with `require_pin=True` so a pageable fallback refuses instead of
  recording the lower-bound shape, and the 119.6 tok/s / 3.32 GB copies in the
  README, the benchmarks index and the perf docs are now labelled pre-repair
  with a pending row reserved in `benchmarks/gate-v0.72.2-nf4.md`. CPU-tested
  protocol guards live in `tests/test_issue361_nf4_throughput.py`.
