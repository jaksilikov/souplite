- **`data.remove_unused_columns` now defaults to `false`, and an explicit `true` loads with a warning and is ignored; Soup v0.76 will refuse it (#759 by @Srinivasan8888 in #1027).** No trainer reads the field. The trainers that set the Hugging Face argument pass `False`, so a custom collator still receives the columns the model's `forward()` does not name, and the old declared default of `true` misdescribed every run. `true` is not refused yet, because `soup autopilot` writes the whole config, so every file it generated on the old default contains `remove_unused_columns: true`; those keep loading, and no run changes behaviour, because the effective value was always `False`. Loading one prints `data.remove_unused_columns: true has no effect and is ignored`, once per load; delete the line before v0.76. The field is not deleted, since an unknown key is now a hard error.
