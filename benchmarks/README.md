# Measurement records

Raw gate records for Soup's layer-streaming feature and its release gate,
published as written.

These are not a report assembled after the fact. They are the working records
kept while each item was built and verified, so they contain the failures, the
assumptions that turned out wrong, and the numbers that were measured and then
discarded — in the order those things happened.

They are the evidence behind the preprint:

> Makazhan, A. (2026). *Exact Layer Streaming: LoRA Fine-Tuning of an 8B Model
> on a 4 GB Laptop GPU.* Zenodo.
> [10.5281/zenodo.21771064](https://doi.org/10.5281/zenodo.21771064)

| File | What it gates | Headline |
|---|---|---|
| [`gate-674-quest-mixed-route.md`](gate-674-quest-mixed-route.md) | Evaluation-only confirmation of the mixed W4/A4+A16 route retained after strict W4A4 failed ([#674](https://github.com/MuhtarJaksilikov/Soup/issues/674)); not training-quality or efficiency evidence | **Gate passed:** the predeclared seven-A16 rescue measured 0.086344 nat [0.080241, 0.093190] above fixed FP over 704 examples / 25,017 targets and improved native W4A4 by -0.015452 [-0.017552, -0.013528]. Three training seeds, one model, fake quantization, zero backwards/updates; FINAL256 remains untouched. |
| [`gate-674-quest-w4a4-sft.md`](gate-674-quest-w4a4-sft.md) | Experimental QuEST W4A4 SFT feasibility ([#674](https://github.com/MuhtarJaksilikov/Soup/issues/674)); not an integration | **Gate failed:** 0.114 nat [0.099, 0.130] above the strongest retained FP on the same panel. One training seed, one model, fake quantization; no parity or efficiency claim. Retains failed directions, costs, source/result hashes and spent-panel history; FINAL256 remains untouched. |
| [`gate-655-pinned-store-accounting.md`](gate-655-pinned-store-accounting.md) | Pinned host-memory accounting and the `/dev/shm` preflight decision ([#655](https://github.com/MuhtarJaksilikov/Soup/issues/655)) | Two pinned 4 GiB allocations on an L4/Linux/CUDA stack each add exactly 4 GiB to `RssShmem` without using `/dev/shm`; the pageable control adds 4 GiB to `RssAnon`. Preserves the original host-counter drift and probe. |
| [`gate-v0.72.0-layer-streaming.md`](gate-v0.72.0-layer-streaming.md) | The streaming path itself | Bit-exactness vs a resident reference; 3B bf16 trained on a 4 GB card |
| [`gate-v0.72.2-nf4.md`](gate-v0.72.2-nf4.md) | NF4 quantised streaming | Llama-3.1-8B at 119.6 tok/s in a 3.32 GB peak |
| [`gate-v0.72.3-breadth.md`](gate-v0.72.3-breadth.md) | Nine architectures, batching, accumulation, resume, disk tier | Peak-VRAM predictor at 0.85% worst-case error; accumulation is per-token I/O-neutral |
| [`gate-v0.72.4-preference-losses.md`](gate-v0.72.4-preference-losses.md) | DPO / ORPO / SimPO / KTO over the streaming engine | DPO's reference model costs no extra weights — 0.914x the SFT peak, against +730.44 MB for a real second instance |
| [`probe-v0.73.0-what-bounds-streaming.md`](probe-v0.73.0-what-bounds-streaming.md) | What the streamed step is actually bound by, and Cut Cross-Entropy on top of it | **Not** transfer-bound: 71.3% of the card's same-session GEMM ceiling, and deleting every host-to-device byte buys 1.4%. CCE triples the usable microbatch for +9.6% |
| [`run-t4-colab-free-tier.md`](run-t4-colab-free-tier.md) | Not a gate — one completed run on hardware the maintainer does not own | An 8B NF4 streamed run finishes on a free-tier Colab **T4 (sm_75, Turing)** inside a 4.00 GB process cap, peak **2.91 GB** against a predicted 3.02 (the estimator over-predicts by 3.8%, the safe direction). A second run on the same card (#844) measured **1.83 GB** peak instead — the embeddings and head moved out of the resident allocation into a streamed large-layer slot between the two runs, which explains both figures; both are recorded. **No throughput is quoted** — a capped card is not a benchmark — and gradient exactness on Turing is *not* shown |
| [`gate-v0.73.1-measured-vram-fit.md`](gate-v0.73.1-measured-vram-fit.md) | Measuring the streaming VRAM fit instead of predicting it | The peak-VRAM formula **under-predicts at long sequence** — 0.934x the real peak at seq 5120 and 0.787x at 6144, measured through the real `soup train`, a direction the v0.72.3 grid could not see because all ten of its rows sit at seq 256 or 512. Carries **three readings that were withdrawn** during the work, including two that looked like the headline result |
| [`gate-395-second-stack-vram.md`](gate-395-second-stack-vram.md) | The VRAM fit on a second GPU/stack — the verification `gate-v0.73.1` could not do on one card | The under-prediction **does not reproduce**: flat 1.16x over-prediction across seq 2048-6144 on two models, against 0.934x / 0.787x on the RTX 3050. The gap is a single stack-specific term, not drift with sequence. The SDPA math-path hypothesis is published as a **negative result** with a positive control, and explicitly *not* extended to the 3050. Carries a **discarded first sweep** whose numbers were an artifact of `max_length` truncating rather than padding |
| [`gate-v0.73.2-leg2-scoring.md`](gate-v0.73.2-leg2-scoring.md) | `soup ship`'s leg-2 scorers — the release gate itself, not streaming | A suite scored **0.000** for a stub answering every item *correctly*, twice over: `\boxed{C}` was unknown to the MCQ extractor, and a tool call one closing brace short fell through to the inner object. Two models with **byte-identical** scores on all seven suites, one refusing every benign request, were indistinguishable to the gate. The measured noise floor on this box is **0.0000** — CPU greedy decode is deterministic, and the H100's 0.015/0.020 is explicitly *not* re-claimed here. Carries a **withdrawn** order-dependence scare, a control that varied nothing, and a review finding that was checked against three implementations and **partly rejected** |
| [`gate-v0.76.0-tool-call-discrimination.md`](gate-v0.76.0-tool-call-discrimination.md) | `mini_tool_call` discrimination after #405 | Qwen2.5-7B-Instruct moves from the shipped 1.000 ceiling to **0.850 (34/40)** while SmolLM2-135M-Instruct scores **0.050 (2/40)**; the record keeps the rejected 0.969 candidate and the provenance-scale change. |
| [`run-m1-8gb-mlx-sft.md`](run-m1-8gb-mlx-sft.md) | Not a gate — the MLX backend's training loop on hardware CI does not have | `MLXSFTTrainerWrapper` runs end to end on an **8 GB M1**, and the shipped `llama3.1-8b-sft-mlx` recipe **fits**: peak **5.154 GB**, 48 iterations in 71 s. A model too large does **not** fail here, it pages — free memory sat at 5-6% while the machine stayed responsive, so an allocation-failure pre-flight would not fire (the #649 trap from the other platform). Carries a **failed fixture** — the model id #23 itself recommends ships legacy `weights.NN.safetensors` and cannot be loaded — and two **corrections** I made mid-measurement |
| [`gate-qwen4-ple-m4-max.md`](gate-qwen4-ple-m4-max.md) | Qwen4-Exp/oQ decoder mapping and read-only PLE on Apple Silicon | **Partial pass only:** 1,167/1,167 cached tensors and tiny CPU/MPS parity passed; the production optimizer step was stopped and is explicitly **not validated** |
| [`gate-h100-validation.md`](gate-h100-validation.md) |  The method on someone else's hardware: bit-exactness at real sizes, convergence quality, DeepSpeed, variance | **Forward** bit-exact to 72B; **backward** bit-exact to 14B NF4 pre-repair, re-gated after the STEP 14 fix at 32B (256/256) **and at 72B (320/320, the size where the defect was worst)**; 2.93x DeepSpeed ZeRO-3 offload in 9.7x less VRAM; and the silent wrong-gradient defect that fix repairs. **Carries three dated 2026-08-13 corrections**: it explains the H100 replication as host-to-device transfer, which the probe record above later measured and refuted. The original lines are left standing with the correction beside them |
| [`probe-rtx5070-what-bounds-streaming.md`](probe-rtx5070-what-bounds-streaming.md) | The FIRST baseline on the new dev box (RTX 5070 Laptop 8 GB, PCIe 5.0 x8, 32 GB RAM, 2x NVMe) — the same "what bounds the streamed step" question as `probe-v0.73.0`, plus the shipped DISK tier warm and cold, through a re-runnable harness | **RAM tier is still compute-bound** (Mistral-7B NF4, batch 1 x 512: 486 tok/s, compute-stream stall 0.34%), but the copy stream is now 42% busy and the **NF4 dequantisation is 23% of the step** (was 9.8%) — #842 is worth a quarter of the step here. The shipped **disk tier is 2.3x slower than RAM with the whole store in the page cache** (the Python thread blocks in `get_tensor`; GPU stall ~0), and **cold on a 36 GB 70B-shaped NF4 store it delivers 4.1 tok/s** — 124 s per step, 0.57 GB/s through mmap page faults on an NVMe that reads at 3.5+ GB/s. Also finds that **the shipped sharder dies on Windows past ~100 GB of live safetensors mappings** (a real 70B is 141 GB), reproduced without Soup (#926); sharded through a documented harness-side workaround **§21 (2026-09-15, on the post-#974 direct-I/O reader):** the cold 70B step is read-bound outright — 17.0–17.2 s at seq 512 AND at seq 256 while the compute floor halves (6.4 → 3.8 s) — so the per-visit NF4 dequantisation (#842: 8–9 ms/visit, 1.3–1.5 s/step, 23% of the compute floor) and the pure per-visit fixed cost (#841: 0.65 ms/visit, ~0.1 s/step) are both hidden inside the read; #841 folds into #842, which is the RAM-tier lever and 0% on the cold disk tier. The step moves 70.4 GB at 4.1 GB/s averaged, 73% of the drive's 5.65 GB/s unbuffered ceiling |
| [`gate-971-async-nvme-source.md`](gate-971-async-nvme-source.md) | The asynchronous disk-tier source ([#971](https://github.com/MuhtarJaksilikov/Soup/issues/971)) — a background reader with its own header parsing (no mmap) and pinned host staging, measured cold and warm against a SAME-DAY control of the source it replaces | **Cold, the step is 2.1x-3.1x faster**: a 70B-shaped NF4 step at batch 1 x 512 goes 100.35 -> 48.09 s early and **92.25 -> 30.28 s** late, each pair position-matched (5.1-5.6 -> 10.6-16.9 tok/s; 0.70-0.76 -> 1.46-2.32 GB/s at the source; 2.59x against the other record's published 124.5 s). **Warm it is a REGRESSION** — 1.03-1.20x slower than the source it replaces with the whole store in the page cache, the two figures being the two run orders, where §8's published before would have made the same number look like a 1.33x improvement. Carries a **depth result that turned out to be an ordering result** (the same config is 48.09 s run first and 30.28 s run last, larger than the whole spread across depths 1/2/4), a **localisation that was wrong and is corrected in place**, an 11.35x instrumented outlier that **did not reproduce**, and a **2026-09-14 revision pass** that re-measured §8 from the committed harness, re-ran the warm trio in reverse order, and dated six corrections where the text claimed more than the JSON supported |
| [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) | The disk tier's reads, warm and cold ([#974](https://github.com/MuhtarJaksilikov/Soup/issues/974)) — the reported warm regression re-measured position-matched, the cold read primitive measured buffered against unbuffered on a store larger than RAM, then the direct-I/O range reader gated in both run orders against the shipped synchronous source AND the reader it replaces (imported from `origin/main`, the tree stamped in every JSON) | **The warm regression does not exist position-matched (0.91–0.97x of the synchronous control before the change, 0.76–0.94x of the reader it replaces after it), and cold the 70B-shaped step is 16.2–17.3 s at BOTH run positions** — 6.4x the synchronous path in the same slot (110.5 s), 2.0–2.8x the reader it replaces (34.8–45.6 s), read share 84.7% -> 62–64% at 6.0–7.0 GB/s, the two positions within 1.07x where the old reader's differed 1.31x; peak VRAM unchanged at 4.38 GB. The reason: cold, every BUFFERED read primitive tops out at 1.2–2.9 GB/s on this NVMe whatever the shape, unbuffered reads of the same layers reach 3.5–5.65. Keeps a first cold series killed by a peer session, an order-B control that starved itself on its own 42 GB commit charge (reported, not built on), the handoff's page-cache hypothesis refuted in 30 s, and a 2.4x-slow block that would not come back until the re-check found the ordering that brings it |
| [`gate-901-14b-on-8gb.md`](gate-901-14b-on-8gb.md) | The #901 report — Qwen2.5-14B streamed as NF4 "OOMs inside `SFTTrainer.__init__`" on an 8 GB card with 32 GB of RAM ([#901](https://github.com/MuhtarJaksilikov/Soup/issues/901)) — reproduced on a synthetic 14B shape, reduced to pure torch, and fixed | **Memory was never the problem.** A failed page-lock of the 9.93 GB store leaves a stale CUDA error that the run's first kernel launch reports as `CUDA error: out of memory` with 7.3 GB free (cudaMalloc, synchronize and memcpy all pass; the launch check reads it), and the page-lock failed because torch's caching host allocator **rounds every pinned request up to a power of two** — the store cost 1.73x its bytes per tensor (a 100 000-byte request shows as 131 072 in `host_memory_stats`). After draining the error and pinning the store in power-of-two arenas the same config trains end to end here: **measured peak 2.944 GB allocated / 3.244 GB reserved** against the pre-flight's 3.39 GB, the store page-locked as exactly 10 GiB (1.08x) where it fell back to pageable before. Carries a **memory hypothesis refuted by a 3 KB allocation failing**, and no throughput or quality claim: the fixture is a shape with random weights |
| [`probe-moe-expert-coverage.md`](probe-moe-expert-coverage.md) | MoE step 0 — is expert-granularity streaming worth building? Streaming one expert at a time instead of one decoder layer only saves READS to the extent that a step touches fewer experts than the layer holds, so this measures union coverage, routing skew and prefetch predictability per layer on two trained MoE models over three corpora. **The decision rule was committed before any model was downloaded** (4908d52b) and the numbers arrived afterwards | **A training step touches essentially every expert: C = 0.967-1.000** over 18 corpus/shape combinations, worst single layer 0.909, against a chance baseline of 1.000 — so the read volume of an expert-streamed step equals that of a layer-streamed one and **the feature is a capacity tier with no throughput gain**, which is the branch the pre-committed rule assigns to `C >= 0.85`. Concentration is real but orthogonal and corpus-dependent (Gini 0.271 on prose to 0.558 on code; the busiest quartile takes 41-67% of traffic), and the rule's hybrid-tier threshold fires only on code at 64 experts. Carries an **arithmetic correction to the rule itself**: at coverage ~1 a resident hot set saves the RESIDENT FRACTION of bytes, not the traffic share those experts carry. NF4 against a bf16 CPU control moves nothing (coverage delta 0.0008, worst layer 0.0068), and the prefetch numbers are reported as uninterpretable at this coverage — which the record said they would be, before the run |
| [`gpu-test-runs.md`](gpu-test-runs.md) | Not a gate — every run of the CUDA-dependent tests (`pytest -m gpu`) on a real card, since CI has none ([#833](https://github.com/MuhtarJaksilikov/Soup/issues/833)) | Two runs recorded, both before the marker existed, and **both found defects CI could not see**: 8x H100 (`9 failed, 419 passed` on the four streaming suites, three defects) and an RTX 5070 Laptop (sm_120, full suite `20709 passed, 2 failed`, #776) |
| [`gate-725-lorafa-operating-point.md`](gate-725-lorafa-operating-point.md) | PEFT LoRA-FA optimizer operating point ([#725](https://github.com/MuhtarJaksilikov/Soup/issues/725)) | Measured 50.0% fewer trainable adapter parameters and 50.0% fewer AdamW state elements; analytic $r / d_{in}$ activation retention for adapted projections. Micro-benchmark only; explicitly not total LLM peak VRAM, throughput, or quality claim. |
| [`gate-836-bench-train-contract.md`](gate-836-bench-train-contract.md) | What `soup bench train` reports on a real card ([#836](https://github.com/MuhtarJaksilikov/Soup/issues/836), shipped in [#1146](https://github.com/MuhtarJaksilikov/Soup/pull/1146)) — the published reference run that acceptance item 7 asked for, on an RTX 5070 Laptop **on battery at a 50 W cap** | **The contract holds; the number does not repeat.** Over **thirteen runs of a byte-identical config** the config hash, token counts, `max_memory_allocated`, `max_memory_reserved` and all three checks are **identical to the byte**, while reported throughput spans **376.4–915.3 supervised tok/s, 2.43x** (CV 35.3%). The noise is **additive, not multiplicative**: across four shapes whose fast-mode step spans 26x (0.197–5.26 s) the fastest and slowest repeat differ by a near-constant **0.17–0.28 s per step** — 4.6% on a 5.3 s step, 1.9x on a 0.2 s one — and a longer window does **not** average it away (160 counted steps still spread 1.89x). Two explanations were tested and **both refuted by their own covariate** (peer processes; CPU frequency, where the highest-frequency run is in the slow half). Also: one shape **completed while allocating 1.51x the card's VRAM** (WDDM spill — `memory` is the allocator's truth, not a fit), `provenance.sm_clock_mhz_after_run` is an **idle reading** that cannot discriminate a 1.83x difference, and `config_hash` changes with `--steps` because the tool's own `save_steps` override is inside it. Four refusals exercised verbatim; one **contaminated run** kept in |

## Harnesses

[`harness/`](harness/) holds the measurement scripts that can be run against a
released Soup, so a claim in a record above can be re-measured rather than taken
on trust. It starts small on purpose — most of this session's ~20 harnesses live
only in a scratchpad on a machine that is gone, which is
[#379](https://github.com/MuhtarJaksilikov/Soup/issues/379).

| Script | STEP / record | Question or claim | Cost / requirements |
|---|---|---|---|
| [`quest_mixed_route.py`](harness/quest_mixed_route.py) | [#674](https://github.com/MuhtarJaksilikov/Soup/issues/674) / [`gate-674-quest-mixed-route.md`](gate-674-quest-mixed-route.md) / [`gate-674-quest-w4a4-sft.md`](gate-674-quest-w4a4-sft.md) | Does the experimental integration reconstruct exactly 168 W4 weights / 161 A4 + 7 block-23 A16 activations, and what dense fake-quant overhead does one A4/A16 layer add on this host? The fixture is synthetic and the JSON explicitly refuses quality, packed-INT4, throughput and memory-saving claims | CPU or CUDA; no downloads; seconds at the default width 128 |
| [`issue331_qlora_scope.py`](harness/issue331_qlora_scope.py) | #331 | Does the wrong-gradient defect reach **ordinary QLoRA**? Three arms in one process, with a positive control. Answer: no — 0.0 against a control that diverges by 3.77e-01 | ~15 s, 4 GB card, no downloads |
| [`bnb_repro.py`](harness/bnb_repro.py) | Upstream bitsandbytes #2034 / NF4 defect | Minimal standalone reproducer for the NF4 stale-gradient mechanism: recycled buffers expose `MatMul4Bit`'s `ctx` lifetime issue; private buffers are the reference and bf16 is the control | CUDA GPU required; no downloads; historical environment: torch 2.13.0+cu130, bitsandbytes 0.50.0; ~1 min |
| [`mechanism_cost.py`](harness/mechanism_cost.py) | STEP 9 / #331 | Measure the recorded NF4 de-aliasing cost on a local Qwen2.5-32B-Instruct checkpoint: `control`, `clone_fwd_quant`, and `clone`, with correctness asserted in-process before timing; `--self-test` exercises the negative empty-intersection acceptance case | CUDA GPU + local checkpoint required; **about 20 GiB GPU memory** for the clone arms; checkpoint must contain real `.safetensors` files (symlinked HF-cache snapshots are refused); no downloads; historical protocol: seq 256, batch 1, 5 repeats, 4 warm-up steps, 15 timed steps, 2 stream buffers |
| [`bitexact.py`](harness/bitexact.py) | v0.72.0 / STEP 1; gate-h100-validation.md Reproducing | Shard -> stream -> compare logits, LoRA gradients, and loss curve against a resident reference using matching numerics; NF4 uses a resident NF4 reference | CUDA GPU required; model/checkpoint weights required; historical run: Qwen2.5-0.5B-Instruct NF4, seq 64; CUDA validation: torch 2.5.1+cu121, transformers 4.57.6, trl 0.19.1, peft 0.18.1 |
| [`fsdp_sharding_probe.py`](harness/fsdp_sharding_probe.py) | STEP 20 / [#373](https://github.com/MuhtarJaksilikov/Soup/issues/373) | Is the base actually **sharded**, or was the flag merely accepted? Per-rank `local == total / world_size` asserted, with the single-GPU control expecting the *full* count so a probe that always divides fails instead of passing everything. Repairs the STEP 20 probe, which raised `TypeError('FullyShardedDataParallel does not support len()')` on all eight ranks at 70B — a `torch.compile` wrapper, not FSDP | Accounting is pure-Python and CPU-tested; a real reading needs the multi-GPU box under test |
| [`mlx_sft_smoke.py`](harness/mlx_sft_smoke.py) | [#23](https://github.com/MuhtarJaksilikov/Soup/issues/23) / [`run-m1-8gb-mlx-sft.md`](run-m1-8gb-mlx-sft.md) | Does `MLXSFTTrainerWrapper.train()` actually run against a real MLX runtime, and what does it peak at? Asserts MLX **dispatch before measuring**, so a transformers-path number cannot be published as an MLX one (#363). Generates its own fixture; prints host memory and swap either side | Apple Silicon + `pip install -e ".[mlx]"`; downloads the chosen model; 20 s at 0.5B, ~6 min at 8B including the Hub fetch |
| [`issue395_second_stack_vram.py`](harness/issue395_second_stack_vram.py) | #395 / [`gate-395-second-stack-vram.md`](gate-395-second-stack-vram.md) | Does the long-sequence under-prediction reproduce on a second stack, and is the gap the logits multiplier? Answer: no and no — the loss term measures 12.000000 (spread 0), and `14 x E` alone exceeds the whole real peak on every row, so the `+2` retained copy is absent. Three modes: sweep, `--measure`, `--sdpa` | 24 GB card (largest arm peaks 11.85 GB); downloads SmolLM2-135M and Qwen2.5-0.5B; needs `jinja2>=3.1.0`; a few minutes |
| [`issue361_nf4_throughput.py`](harness/issue361_nf4_throughput.py) | [#361](https://github.com/MuhtarJaksilikov/Soup/issues/361) / [`gate-v0.72.2-nf4.md`](gate-v0.72.2-nf4.md) | Re-run the v0.72.2 8B NF4 protocol on repaired code (batch 1, S=512, 50 steps after 10 warm-up, `PagedAdamW8bit`, same-session GEMM ceiling)? **No post-repair number yet** — the harness is the protocol, awaiting the RTX 3050 run | RTX 3050 Laptop 4 GB; Llama-3.1-8B weights; CPU-tested protocol guards only |
| [`stream_probe.py`](harness/stream_probe.py) | [`probe-rtx5070-what-bounds-streaming.md`](probe-rtx5070-what-bounds-streaming.md), [`gate-971-async-nvme-source.md`](gate-971-async-nvme-source.md) | What bounds the streamed step on THIS card? Builds the model through the shipped path, replicates the two shipped copy paths with CUDA events added (a source guard refuses to run if the shipped body drifts), and runs the ceiling / step / sweep / interleaved-ablation modes of the v0.73.0 probe, writing JSON after every point. `--tier disk` measures the ASYNC source (#971) at the depth `--read-ahead N` sets, and `--control-sync-source` swaps in the shipped synchronous `DiskSource` for a same-session control; every run prints and records the source class it actually built and refuses when that disagrees with the flag, so a control cannot be published as a measurement. `--no-pin` selects pageable host memory on EITHER tier. `--lazy-shard-handles` **was** the sharding workaround for the Windows mapping limit and is no longer needed: [#934](https://github.com/MuhtarJaksilikov/Soup/pull/934) (merged 2026-09-14) bounds the shipped sharder's live mappings, and its gate re-sharded the 138 GB 70B-shaped fixture with no workaround at all — 80/80 layers in 221.4 s against 216.9 s with the flag, and 83 output files / 36.39 GB byte-identical to the workaround's cache, `index.json` included. The flag is kept so the 2026-09-12 record can be re-run as it was measured. Since #974 every JSON's `meta` also records `souplite_file`, the source tree the run actually imported — an arm that claims to be "the old code" is only evidence if the JSON says where it came from — and `--skip-events` runs only the uninstrumented `step_plain` point (halves a cold 70B block; not used by the #974 record, whose blocks run both points as gate-971 did) | CUDA GPU; model weights; a 7B NF4 run is ~10 min all modes; the 70B-shaped cold-disk run is 10-60 min |
| [`layer0_wait.py`](harness/layer0_wait.py) | [`gate-971-async-nvme-source.md`](gate-971-async-nvme-source.md) §8 | Which layer pays for the vocabulary-sized read planned at the head of every step? `stream_probe.py` brackets every load with CUDA events but SUMS them per step; this drives the probe's own instruments from outside and labels each bracket with its layer. Answer: **layer 0's forward load is the fastest of the step** (16-17 ms against a 213-252 ms mean), because the reader stages it during the build and embedding phase — so the behaviour is not material and needs no fix. The largest single compute-stream stall is the **vocabulary fetch itself**, 32.6-32.9 ms in every step against layer 0's 0.17/14.99/14.35 ms and a 0.002 ms mean over the other 79 decoder layers: the whole step-head stall is 33/48/48 ms, 0.07-0.12% of the step, so it does not change the answer either. Instruments from step 0 with no warm-up, so its absolute step times are deliberately NOT throughput evidence | CUDA GPU; model weights; minutes |
| [`synth_checkpoint.py`](harness/synth_checkpoint.py) | [`probe-rtx5070-what-bounds-streaming.md`](probe-rtx5070-what-bounds-streaming.md) §3; [`gate-901-14b-on-8gb.md`](gate-901-14b-on-8gb.md) §1 | Writes a Llama- or Qwen2-SHAPED synthetic checkpoint (random N(0, 0.02) weights, real shapes) so a store larger than RAM can be built without a Hub token or a 140 GB download, and so an out-of-memory report can be reproduced from shapes alone (`--shape qwen2.5-14b --arch qwen2` is #901's). Timing and memory only — never a correctness or quality claim | ~155 s and 138 GB for the 70B shape, 29.5 GB for the 14B one; needs a GPU for the random draw to be fast |
| [`issue901_stream_step_probe.py`](harness/issue901_stream_step_probe.py) | [`gate-901-14b-on-8gb.md`](gate-901-14b-on-8gb.md) §2, §4 | Does this shape fit, and where does the memory go? Builds the streamed model through the shipped path (`stream_probe.build`), runs ONE forward+backward at the configured shape, and marks `memory_allocated` / peak at the embedding, at chosen decoder layers (forward and the start of their backward), at `lm_head` and at the loss, with the allocator history recorded so an OOM is attributed to live blocks rather than guessed. Answer for #901: the first CUDA op after the build failed at 3 KB with 5.4 GB free — not memory — and after the fix the 14B NF4 step peaks at 2.944 GB | CUDA GPU; model weights (the synthetic 14B shape serves); ~1 min on a cached shard set, +1 min to shard |
| [`mmap_limit_repro.py`](harness/mmap_limit_repro.py) | [#926](https://github.com/MuhtarJaksilikov/Soup/issues/926) / [`probe-rtx5070-what-bounds-streaming.md`](probe-rtx5070-what-bounds-streaming.md) §13–§14 | Does the sharder's all-handles-open pattern die on its own, without Soup, CUDA or bitsandbytes? Two modes over one directory of `*.safetensors`: `all` holds every handle open, `perfile` opens, reads and closes each in turn. On the Windows box `all` dies once ~96–106 GB of mappings are live, at 14 handles or at 62; `perfile` passes. `all` **was** the shipped pass-2 pattern when this was written; since [#934](https://github.com/MuhtarJaksilikov/Soup/pull/934) (merged 2026-09-14, closing #926) the sharder keeps at most two source handles alive and every read owns its memory, so `all` now reproduces the historical defect rather than the current code | Any box; a directory of large safetensors totalling more than ~100 GB to see the crash (the synthetic 70B shape serves); seconds — it maps, it does not read unless `--touch` |
| [`moe_expert_coverage.py`](harness/moe_expert_coverage.py) | [`probe-moe-expert-coverage.md`](probe-moe-expert-coverage.md) | Does one training step touch every expert of a MoE layer? Expert-granularity streaming only saves READS to the extent that it does not. Hooks every top-k router and reports, per layer: union coverage, routing skew (Gini plus the share taken by the busiest 10/25/50%), hot-set stability, and the share of the next layer's traffic that prefetching either the previous layer's used set or a corpus-wide heat quartile would have caught. Every coverage figure is printed beside the uniform-random baseline `1 - (1 - k/E)^T` for the same shape, because coverage alone is not interpretable: at 64 experts / top-8 over 512 tokens chance alone predicts `1 - 10^-29.7`, so a **high coverage is the default rather than a finding**. Router discovery is by SHAPE, not an architecture table — `granitemoe` returns the indices FIRST where olmoe, qwen3_moe and mixtral return them last, and both are validated end to end. Tokens are packed, never padded, since a padded batch routes its PAD positions too. No `souplite` import | Any box for the validation (it builds its own 6.6M-param models); a real reading needs the model weights and, for a 7B-class MoE, either a CUDA card with `--load-4bit` or ~14 GB of host RAM |
| [`issue974_warm_stages.py`](harness/issue974_warm_stages.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §1, §2, §6 | Where does a WARM disk-tier step spend its time, stage by stage? `stream_probe.build` plus host wall-clock timers around each stage: `consumer_wait` (the compute thread blocked in `AsyncDiskSource.get`), `reader_read` (one bracket per layer read — `_read_layer` — so the K parallel ranges are one number, not K overlapping ones), `reader_drain` (the pool event the reader waits on before refilling a slot), `consumer_read` and `consumer_load_async` for the synchronous control. One arm per process, so run order is explicit. Answer: position-matched, the async source was 0.91-0.97x the control before #974 — the 1.03-1.20x was run order — and the drain is 1-8 ms/step | CUDA GPU; the Mistral-7B NF4 shard cache; ~40 s per arm |
| [`issue974_cache_populate.py`](harness/issue974_cache_populate.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §2 | Does a buffered `readinto` repopulate the Windows page cache, or only mmap? Runs the two primitives over the same shards in a chosen order with an eviction where the sequence says, and reports GB/s per pass. Answer: every `readinto` flavour (plain / `O_RANDOM` / `O_SEQUENTIAL` / `buffering=0`) warms the cache on its second pass (2.60 -> 3.96 GB/s); the handoff's hypothesis was wrong | CUDA GPU (pinned staging); a shard cache; ~1 min |
| [`issue974_evict.py`](harness/issue974_evict.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §2, §4 | Evict a store from the page cache without admin rights: touch ~17 GB of pageable RAM (available physical falls to ~2.5 GB), then read 8 GB of OTHER files through buffered readinto to displace the survivors — pressure alone leaves the most recently used ~2.5 GB of the standby list resident, i.e. exactly the store under test. Prints the host baseline around both stages | Any Windows box; `--displace-dir` must name large files unrelated to the store; ~15 s |
| [`issue974_cold_ranges.py`](harness/issue974_cold_ranges.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §4 | How fast can the disk tier read a layer that is NOT in the page cache, through BUFFERED reads? Every (mode, threads) configuration reads its own fresh layers of a store larger than RAM, in two configuration orders. Answer: 1.25-2.4 GB/s whatever the shape — per-tensor readinto, one readinto per range, 1-8 threads — the cache manager is the ceiling | CUDA GPU (pinned staging); the 70B-shaped NF4 cache (34 GB > RAM); run `issue974_evict.py` first; ~1 min |
| [`issue974_unbuffered.py`](harness/issue974_unbuffered.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §4 | Does UNBUFFERED I/O (`FILE_FLAG_NO_BUFFERING`, one handle per thread, into a 4 KiB-aligned pinned arena over the aligned superset of the data section) reach the drive? Same fresh-layers-per-configuration policy, buffered arm as the control, byte check on the last layer. Answer: 3.5-5.65 GB/s cold, best at 2-4 ranges, against 1.2-2.9 buffered in the same positions — the number the #974 reader is built on. Carries the alignment rules the reader inherits (an unaligned LENGTH is EINVAL; the last request of every layer runs past EOF and returns short) | Windows (ctypes `CreateFileW`); CUDA GPU; the 70B-shaped NF4 cache; run `issue974_evict.py` first; ~1 min |
| [`issue974_read_paths.py`](harness/issue974_read_paths.py) | [`gate-974-disk-tier-direct-io.md`](gate-974-disk-tier-direct-io.md) §3 | The WARM read-primitive micro-benchmark that motivated the first fix plan: per-tensor readinto vs mmap+copy vs one readinto per layer of the whole data section into an arena, split across K threads, store in the page cache. Answer: 3.9 -> 9.3 GB/s at 8 threads for the arena read — a page-cache -> pinned memcpy number, which §4 shows says nothing about cold. Kept because a record that changed its plan should show the number it changed it from | CUDA GPU; the Mistral-7B NF4 cache; ~30 s |
| [issue725_lorafa_operating_point.py](harness/issue725_lorafa_operating_point.py) | #725 / [gate-725-lorafa-operating-point.md](gate-725-lorafa-operating-point.md) | Measures trainable parameter counts, AdamW state tensor allocations, step latency, and multi-step loss finiteness between LoRA and LoRA-FA | ~20 s, CPU or GPU, no downloads |

## Hardware

Every number in the four `gate-v0.72.*` records was measured on one machine:

- **GPU** — RTX 3050 Laptop, 4 GB (4.29 GB usable)
- **Host** — 16.9 GB RAM, NVMe
- **OS** — Windows 11

`gate-h100-validation.md` is the exception and the reason it exists: 8x H100
80 GB, 503 GB RAM, Ubuntu 24.04, on a much newer torch/bitsandbytes/trl/peft
stack. It is the first record from hardware other than the laptop, and the first
able to hold a *resident* reference for an 8B–72B model — which is what turns
"bit-exact on a 3-layer toy" into "bit-exact on real models".

`gate-395-second-stack-vram.md` is the third box: an **NVIDIA A10G 23 GB**
(AWS `g5.xlarge`), Ubuntu 22.04, torch 2.13.0+cu130 / transformers 5.16.1 /
trl 0.29.1 / peft 0.20.0. It exists to answer a question one card cannot —
whether the long-sequence under-prediction is a property of the formula or of
the stack it was fitted on. Note the direction of the dependency floor:
`pyproject.toml [train]` now requires `transformers>=5.16.1`, `trl>=0.29.0`,
`peft>=0.20.0`, so this stack **is** that floor while the RTX 3050 stack below
is beneath it.

`run-t4-colab-free-tier.md` is the second exception and a much weaker one: a free
Colab **Tesla T4 (sm_75, Turing)**, one run, no repeats, no captured
correctness comparison, on a session that cannot be returned to. It is filed here
because it is the only evidence that the streaming path executes at all on a
pre-Ampere card, not because it gates anything.

`probe-rtx5070-what-bounds-streaming.md` is the **new dev box** as of 2026-09-10: an
**RTX 5070 Laptop GPU (Blackwell sm_120, 8 GB)** on a PCIe 5.0 x8 link, i9-14900HX,
31.7 GB DDR5-5600, two Samsung PM9B1 NVMe, Windows 11, torch 2.14.0+cu130 /
transformers 5.17.0 / peft 0.20.0 / bitsandbytes 0.50.2. Twice the VRAM and ~4.2x
the GEMM ceiling of the RTX 3050, so **nothing in it is comparable to the four
`gate-v0.72.*` records** — it is a new baseline, and the file says so in its title.

`gate-836-bench-train-contract.md` is the same box a fortnight later and adds one
line to its description that turned out to matter: it ran **on battery**, with the
GPU's enforced power limit at **50.00 W against a 115.00 W maximum**. Nothing in
that record is a statement about what this card does on mains, and a re-run on
mains is a new baseline rather than a comparison.

Windows/WDDM matters for reading these: it spills into shared host memory rather
than raising `CUDA out of memory`, so a run completing is not evidence that its
configuration fits. That is why peak VRAM is reported alongside every throughput
figure, and why the fit decision refuses rather than warns.

## Reading the numbers

- **Throughput is quoted with the SM clock it was taken at.** This card's boost
  clock varies about 13% between sessions, so a fraction-of-ceiling stated
  without its clock is not meaningful. Where a GEMM ceiling is compared against,
  it was measured in the same session.
- **The correctness reference always matches the numerics under test** — a
  streamed NF4 run is compared against a *resident NF4* run, never against
  resident bf16, which would hide a real defect inside quantisation error.
- **"Bit-exact" is always two claims, never one.** The **forward** (logits,
  `torch.equal`) and the **backward** (every LoRA gradient tensor) are measured
  independently and do not always agree: in `gate-h100-validation.md` the forward
  is exact at every size up to 72B while the backward, pre-repair, was wrong above
  ~165 MiB per NF4 layer. So "bit-exact at 72B" on its own is not a statement this
  record makes — check which half, at which quantisation, at which MiB/layer. That
  file opens with a per-model ledger giving all four for every row, and marks
  anything unmeasured "not tested" rather than leaving it blank.
- **Derived figures are labelled as arithmetic.** Where a line says "1M tokens =
  2.3 h", that is division, not a measured wall-clock run.

## Reproducing

The implementation ships in Soup under Apache-2.0. Reproduction commands are in
Appendix A of the paper; the correctness protocol runs as part of the project's
test suite, so a regression in bit-exactness fails CI rather than reaching a
user.

```bash
pip install "souplite[train]"
```
